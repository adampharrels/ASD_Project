<?xml version="1.0" encoding="UTF-8"?><?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0"

<project xmlns="http://maven.apache.org/POM/4.0.0"

         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"<project xmlns="http://maven.apache.org/POM/4.0.0"         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0



    <groupId>com.example</groupId>         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <artifactId>asd-project</artifactId>

    <version>0.0.1-SNAPSHOT</version>         http://maven.apache.org/xsd/maven-4.0.0.xsd">    <modelVersion>4.0.0</modelVersion>

    <packaging>jar</packaging>

    <modelVersion>4.0.0</modelVersion>    

    <name>ASD Project</name>

    <description>Room Booking System</description>    <parent>



    <parent>    <groupId>com.example</groupId>        <groupId>org.springframework.boot</groupId>

        <groupId>org.springframework.boot</groupId>

        <artifactId>spring-boot-starter-parent</artifactId>    <artifactId>asd-project</artifactId>        <artifactId>spring-boot-starter-parent</artifactId>

        <version>3.2.0</version>

        <relativePath/>    <version>0.0.1-SNAPSHOT</version>        <version>3.2.0</version>

    </parent>

    <packaging>jar</packaging>        <relativePath/>

    <properties>

        <java.version>17</java.version>    </parent>

        <maven.compiler.source>17</maven.compiler.source>

        <maven.compiler.target>17</maven.compiler.target>    <name>ASD Project</name>    

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    </properties>    <description>Room Booking System</description>    <groupId>com.example</groupId>



    <dependencies>    <artifactId>room-booking-system</artifactId>

        <dependency>

            <groupId>org.springframework.boot</groupId>    <parent>    <version>1.0-SNAPSHOT</version>

            <artifactId>spring-boot-starter-web</artifactId>

        </dependency>        <groupId>org.springframework.boot</groupId>    <packaging>jar</packaging>

        

        <dependency>        <artifactId>spring-boot-starter-parent</artifactId>    

            <groupId>mysql</groupId>

            <artifactId>mysql-connector-java</artifactId>        <version>3.2.0</version>    <properties>

            <version>8.0.33</version>

        </dependency>        <relativePath/>        <maven.compiler.source>24</maven.compiler.source>

        

        <dependency>    </parent>        <maven.compiler.target>24</maven.compiler.target>

            <groupId>org.springframework.boot</groupId>

            <artifactId>spring-boot-starter-test</artifactId>        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

            <scope>test</scope>

        </dependency>    <properties>    </properties>

    </dependencies>

        <java.version>17</java.version>    

    <build>

        <plugins>        <maven.compiler.source>17</maven.compiler.source>    <dependencies>

            <plugin>

                <groupId>org.springframework.boot</groupId>        <maven.compiler.target>17</maven.compiler.target>        <!-- Spring Boot Web Starter -->

                <artifactId>spring-boot-maven-plugin</artifactId>

            </plugin>        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>        <dependency>

        </plugins>

    </build>    </properties>            <groupId>org.springframework.boot</groupId>

</project>
            <artifactId>spring-boot-starter-web</artifactId>

    <dependencies>        </dependency>

        <dependency>        

            <groupId>org.springframework.boot</groupId>        <!-- MySQL JDBC driver -->

            <artifactId>spring-boot-starter-web</artifactId>        <dependency>

        </dependency>            <groupId>com.mysql</groupId>

                    <artifactId>mysql-connector-j</artifactId>

        <dependency>            <scope>runtime</scope>

            <groupId>mysql</groupId>        </dependency>

            <artifactId>mysql-connector-java</artifactId>        

            <version>8.0.33</version>        <!-- Spring Boot Test Starter -->

        </dependency>        <dependency>

                    <groupId>org.springframework.boot</groupId>

        <dependency>            <artifactId>spring-boot-starter-test</artifactId>

            <groupId>org.springframework.boot</groupId>            <scope>test</scope>

            <artifactId>spring-boot-starter-test</artifactId>        </dependency>

            <scope>test</scope>        

        </dependency>        <!-- H2 Database for testing -->

    </dependencies>        <dependency>

            <groupId>com.h2database</groupId>

    <build>            <artifactId>h2</artifactId>

        <plugins>            <scope>test</scope>

            <plugin>        </dependency>

                <groupId>org.springframework.boot</groupId>    </dependencies>

                <artifactId>spring-boot-maven-plugin</artifactId>    

            </plugin>    <build>

                    <plugins>

            <plugin>            <!-- Spring Boot Maven Plugin -->

                <groupId>org.apache.maven.plugins</groupId>            <plugin>

                <artifactId>maven-compiler-plugin</artifactId>                <groupId>org.springframework.boot</groupId>

                <version>3.11.0</version>                <artifactId>spring-boot-maven-plugin</artifactId>

                <configuration>            </plugin>

                    <source>17</source>            

                    <target>17</target>            <!-- Maven Compiler Plugin -->

                    <compilerArgs>            <plugin>

                        <arg>--enable-preview</arg>                <groupId>org.apache.maven.plugins</groupId>

                    </compilerArgs>                <artifactId>maven-compiler-plugin</artifactId>

                </configuration>                <version>3.11.0</version>

            </plugin>                <configuration>

        </plugins>                    <source>24</source>

    </build>                    <target>24</target>

</project>                </configuration>
            </plugin>
            
            <!-- Maven Surefire Plugin for Tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <testFailureIgnore>false</testFailureIgnore>
                    <reportsDirectory>${project.build.directory}/surefire-reports</reportsDirectory>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
